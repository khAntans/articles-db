<head>
    <meta charset="UTF-8">
    {% if title == true %}
    <title>{{ title }}</title>
    {% else %}
    <title>Articles</title>
    {% endif %}
    <link rel="stylesheet" type="text/css" href="/styles.css" />
</head>